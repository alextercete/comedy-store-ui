(function () {
    'use strict';

    angular
        .module('comedyStore')
        .controller('UserLoginViewModel', UserLoginViewModel);

    UserLoginViewModel.$inject = [];

    function UserLoginViewModel() {
        var viewModel;

        viewModel = this;
    }
})();
